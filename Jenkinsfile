pipeline {
    agent any 
    stages {
        stage('Deploy') { 
            steps {
                echo 'deployeing fucker'
                dir('/home/ec2-user/'){
                    sh 'sudo mkdir test1'
                }
                echo 'deployed'
            }
        }
    }
}
